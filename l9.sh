#!/bin/bash
##############################
#Problem: Write a script `time_command.sh` that takes any command and its arguments (e.g., `./time_command.sh sleep 1.5`), executes it, captures its exact exit code, and prints the duration in milliseconds.
##############################


